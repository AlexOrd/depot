<!--blink add-->
<% if line_item == @current_item %> 
<tr id="current_item">
<% else %>
<tr class="active">
<% end %>
<!---->
    <td><%= line_item.product.title.split[0] %></td>
    <td><%= line_item.quantity %> шт. </td>
    <td class="item_price"><%= number_to_currency(line_item.total_price) %></td>
    <!--button-->
    <td align="center"><%= button_to 'x', line_item, :method => :delete,
            class: "btn btn-danger btn-xs", remote: true %></td>
</tr>
